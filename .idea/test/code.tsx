'use client'

import { Button } from '@nextui-org/react';
import React, { useState } from 'react';

export default function login()
{
    return(
        <Button 
        className="mb-3 w-full py-3 text-white text-lg rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
      >
        一个按钮
      </Button>
    )
}